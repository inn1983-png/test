任务清单 — AI 短剧工厂 Web UI 正式化
========================================

状态：✅ 全部完成（2026-05-08）

一、后端 server.py 正式支持
--------------------------
✅ 已完成。修改内容：

1. build_command 新增 to_module 参数支持
   - 在 from_module/only_module 循环中增加 to_module 映射到 --to-module
   - 文件：web_ui/server.py

2. style_preset 支持
   - build_command 中读取 payload.style_preset，设置环境变量 AI_DRAMA_STYLE_PRESET 和 AI_DRAMA_SELECTED_STYLE_PRESET
   - 文件：web_ui/server.py

3. 超时默认值
   - build_command 中为未设置的超时环境变量填充默认值：
     - AI_DRAMA_LLM_TIMEOUT_SEC=6000
     - AI_DRAMA_IMAGE_COMFYUI_TIMEOUT_SEC=7200
     - AI_DRAMA_VIDEO_COMFYUI_TIMEOUT_SEC=14400
     - AI_DRAMA_COMFYUI_POLL_INTERVAL_SEC=5
     - AI_DRAMA_VIDEO_DRY_RUN_PLACEHOLDER_BYTES=2048
   - 文件：web_ui/server.py

4. /api/review/rewrite 和 /api/review/apply API
   - 新增 _handle_review_rewrite() 方法：接收 run_dir/module_name/user_note，调用 result_rewriter.rewrite_module_result()
   - 新增 _handle_review_apply() 方法：接收 run_dir/module_name/reviewed_path，调用 result_rewriter.apply_reviewed_result()
   - 文件：web_ui/server.py

二、前端 app.js 正式支持成果工作台
---------------------------------
✅ 已完成。将所有 patch 文件逻辑合并到 app.js：

1. 风格选择器（原 style_picker.js）
   - 18 种风格预设列表 STYLE_PRESETS
   - renderStylePicker() 动态插入风格选择下拉框
   - getSelectedStylePreset() 读取选择值，持久化到 localStorage
   - getPayload() 自动包含 style_preset

2. 文本链路按钮（原 text_chain_patch.js）
   - startTextBtn 绑定：from_module=00_style_system, to_module=06_storyboard, 图片/音频/视频=dry_run

3. 成果摘要（原 result_summary.js）
   - MODULE_RESULT_FILES / MODULE_RESULT_TITLES 映射
   - renderResultSummary() 读取模块关键产物 JSON 并渲染可读摘要
   - summarizeModuleResult() 按模块分发摘要渲染
   - 10 个模块专用摘要函数：summarizeStyle/Novel/Script/Characters/Scenes/Props/Storyboard/Images/Audio/Video/Final
   - 辅助函数：rMetric/rChips/rTable/rGet/rArr/rShort/rCount/rGenericArray

4. 修改意见（原 review_notes.js）
   - renderReviewBox() 渲染修改意见文本框
   - callLLMRewrite() 调用 /api/review/rewrite
   - applyLatestReviewedResult() 调用 /api/review/apply
   - 意见持久化到 localStorage

5. 全流程按钮改为从 00_style_system 开始
   - startFullBtn: from_module=00_style_system

6. selectModule() 点击模块节点切换到成果详情标签

7. ISSUE_TYPE_NAMES / ACTION_NAMES 中文映射

8. 文件：web_ui/static/app.js（完整重写，855 行）

三、index.html / styles.css 正式整理
------------------------------------
✅ 已完成。

1. index.html 移除所有 patch 脚本引用
   - 删除：style_picker.js, app_globals.js, text_chain_patch.js, result_summary.js, review_notes.js, workbench_patch.js
   - 只保留：<script src="/app.js"></script>
   - 移除 result_summary.css 单独引用

2. styles.css 合并 result_summary.css 样式
   - 新增：module-result-summary, result-header, result-card, result-metrics, result-metric, result-chips, result-table, review-box, review-actions, style-preset-hint 等样式
   - 合并 @media 响应式规则

四、数据链路完整性检查脚本
-------------------------
✅ 已确认存在，无需新建。
   - 00_main_controller/check_data_link.py 已实现完整功能
   - server.py 中 /api/system/check-data-link API 已接入
   - data_link_btn 前端按钮已绑定

五、超时配置正式化
-----------------
✅ 已确认。
   - 07_storyboard_image/core/comfyui_client.py: timeout_sec 默认 7200s
   - 09_video/core/comfyui_client.py: timeout_sec 默认 14400s
   - server.py build_command 填充默认值

六、result_rewriter.py 逻辑完整性
---------------------------------
✅ 已确认。逻辑完整：
   - rewrite_module_result(): 读取模块产物 → 调用 LLM 修正 → 保存修正版到 reviews/ 目录
   - apply_reviewed_result(): 备份原文件 → 用修正版覆盖
   - 安全措施：路径必须在 workspace 内、_compact_for_llm 处理大文件、保留备份

七、删除临时 patch 文件
-----------------------
✅ 已删除以下 8 个文件：
   - web_ui/static/app_globals.js
   - web_ui/static/text_chain_patch.js
   - web_ui/static/workbench_patch.js
   - web_ui/static/style_picker.js
   - web_ui/static/result_summary.js
   - web_ui/static/review_notes.js
   - web_ui/static/result_summary.css
   - web_ui/sitecustomize.py

八、本地测试验证
---------------
✅ 已通过。
   - Python 编译检查：server.py, result_rewriter.py, repair_index.py, run_pipeline.py, 01-06 stage_runner.py 全部通过
   - Web UI 启动测试：http://127.0.0.1:1144 正常加载
   - 浏览器验证：风格选择器(18种)、分步运行(00-10)、快捷操作、流水线可视化、成果工作台、已有项目列表均正常

九、最终交付
-----------
✅ 所有任务已完成。
